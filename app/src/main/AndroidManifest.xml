<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.vernehelper">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Gallery" />
        <activity android:name=".TextEditorOnGalleryPC" />
        <activity android:name=".PhotoCardCreate" />
        <activity android:name=".EyeExercise" />
        <activity android:name=".EyeSettings" />
        <activity android:name=".PushNotifActivity" />
        <activity android:name=".DiseaseInfo" />
        <activity android:name=".GoodWorkActivity" />
        <activity android:name=".imageList.ImageList" />
        <receiver android:name=".Notification_reciever"/>
        <receiver android:name=".Notificatio_reciever_for_eyes"/>
    </application>

</manifest>